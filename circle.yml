machine:
  services:
    - docker
dependencies:
  cache_directories:
    - "~/docker-ubuntu"
  override:
    - docker info
    - if [[ -e ~/docker-ubuntu/image.tar ]]; then docker load --input ~/docker-ubuntu/image.tar; fi
    - docker build -t hub.goodrain.com/ubuntu .
    - mkdir -p ~/docker-ubuntu; docker save --output ~/docker-ubuntu/image.tar hub.goodrain.com/ubuntu
test:
  override:
    - docker run -it hub.goodrain.com/ubuntu echo "Hello World"
